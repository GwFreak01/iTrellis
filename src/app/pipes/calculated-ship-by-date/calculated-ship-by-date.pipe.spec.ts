import { CalculatedShipByDatePipe } from './calculated-ship-by-date.pipe';

describe('CalculatedShipByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatedShipByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
